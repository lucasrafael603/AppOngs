/*import React, {useState} from 'react'; //Para trabalhar com estados import useState
import Header from './Header'

function App() {

  //Quando atribui o useState a uma variavel ele retorna um Array(Com valor, e uma funçãoDeAtualização)
  let [counter, setCounter] = useState(0) // Não existe a possibilidade alterar a variavel do estado de forma direta, somento por reposição de valores

  

  function increment(){

    setCounter(counter + 1)

    

  }

  //title = "Semana Omnistack" - Caso querer criar propriedade especifica para o componente definir propriedade com o respectivo valor conforme o exemplo.

  return (
    <div>
    <Header> Contador: {counter}  </Header>              
    <button onClick={increment} >Incrementar</button>
    </div>
  );
}

export default App;*/
